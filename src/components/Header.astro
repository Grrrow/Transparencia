---

---

<header class="header">
    <div class="container">
        <a href="/" class="logo">
            <span class="logo-icon">🏛️</span>
            Transparencia Política
        </a>
        <nav class="nav">
            <a href="/" class="nav-link">Inicio</a>
            <a href="/politicos" class="nav-link">Políticos</a>
            <a href="/leyes" class="nav-link">Leyes</a>
        </nav>
    </div>
</header>

<style>
    .header {
        background: var(--color-surface);
        border-bottom: 1px solid var(--color-border);
        position: sticky;
        top: 0;
        z-index: 100;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 1rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .logo {
        font-weight: 700;
        font-size: 1.25rem;
        color: var(--color-text);
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .nav {
        display: flex;
        gap: 1.5rem;
    }

    .nav-link {
        color: var(--color-text-muted);
        font-weight: 500;
    }

    .nav-link:hover {
        color: var(--color-primary);
    }

    @media (max-width: 768px) {
        .container {
            flex-direction: column;
            gap: 1rem;
        }
    }
</style>
